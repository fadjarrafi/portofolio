---
import { getCollection } from 'astro:content';

const allStudies = await getCollection('case-studies', ({ data }) => data.featured === true);
const studies = allStudies
  .sort((a, b) => new Date(b.data.publishedAt).getTime() - new Date(a.data.publishedAt).getTime())
  .slice(0, 3);

if (studies.length === 0) return;
---

<div>
  <div class="flex items-baseline justify-between mb-6">
    <h2 class="text-sm font-medium text-neutral-500 dark:text-neutral-400">Case Studies</h2>
    <a
      href="/case-studies"
      class="text-xs text-neutral-500 dark:text-neutral-400 hover:text-neutral-900 dark:hover:text-neutral-100 transition-colors"
    >
      View all →
    </a>
  </div>
  <div class="grid gap-4 sm:grid-cols-2">
    {studies.map((study) => (
      <a href={`/case-studies/${study.id.replace(/\.mdx?$/, '')}`} class="group block">
        <article class="h-full p-4 border border-neutral-200 dark:border-neutral-800 rounded-lg hover:border-neutral-400 dark:hover:border-neutral-600 transition-colors">
          <div class="min-w-0">
            <h3 class="font-medium text-neutral-900 dark:text-neutral-100 mb-1 line-clamp-2 group-hover:text-neutral-600 dark:group-hover:text-neutral-400 transition-colors">
              {study.data.title}
            </h3>
            <p class="text-sm text-neutral-600 dark:text-neutral-400 line-clamp-2">{study.data.summary}</p>
            {study.data.topics && study.data.topics.length > 0 && (
              <div class="flex flex-wrap gap-1 mt-3">
                {study.data.topics.slice(0, 3).map((topic) => (
                  <span class="px-2 py-0.5 text-xs bg-neutral-100 dark:bg-neutral-800 rounded text-[#6b7a00] dark:text-[#BDDB0B]">
                    {topic}
                  </span>
                ))}
              </div>
            )}
          </div>
        </article>
      </a>
    ))}
  </div>
</div>
